services:
  warp:
    image: ghcr.io/kingcc/warproxy:latest
    environment:
      - SOCKS5_PORT=1080
    volumes:
      - ./config:/config  # Сохраняем учетку WARP, чтобы не регистрироваться заново при перезапуске
    ports:
      - "0.0.0.0:1080:1080"
